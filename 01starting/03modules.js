const temp  = require('./02people');
// const {people, ages} = require('./02people'); // this is called object destructuring
// console.log(temp);

const os = require('os');

// console.log(os); //os is a object which has many functions and properties.

console.log(os.platform(), os.homedir()); // this will give the platform of the os and the home directory of the os.